# Boswell Railway Configuration
#
# Uses SERVICE_TYPE env var to determine web vs worker mode.
# Set SERVICE_TYPE=worker on the worker service.

[build]
builder = "dockerfile"

[deploy]
restartPolicyType = "always"
